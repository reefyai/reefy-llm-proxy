fastapi==0.115.0
uvicorn[standard]==0.32.0
httpx==0.27.2
# inotify-backed file watcher (Rust core). Used in main.py to
# auto-reload the credential vault when the attach file changes.
watchfiles==0.24.0
